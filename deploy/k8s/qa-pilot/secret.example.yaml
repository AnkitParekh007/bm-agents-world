# TEMPLATE ONLY. Do not commit real secret values and do not apply this file unchanged.
apiVersion: v1
kind: Secret
metadata:
  name: bm-agents-world-secrets
  namespace: bm-agents-world
type: Opaque
stringData:
  OPENAI_API_KEY: "REPLACE_WITH_SECRET_MANAGER_VALUE"
  # Server-only shared persistence. Prefer a Supabase direct/session-pooler
  # Postgres connection string appropriate for your network/runtime.
  BM_POSTGRES_URL: "REPLACE_WITH_POSTGRES_CONNECTION_STRING"
  SUPABASE_URL: "https://REPLACE_WITH_PROJECT_REF.supabase.co"
  # Use the server-side sb_secret_... key. Never expose it to browser code,
  # CopilotKit model context, logs, or client-visible configuration.
  SUPABASE_SECRET_KEY: "REPLACE_WITH_SB_SECRET_KEY"
  QA_JIRA_EMAIL: "REPLACE_WITH_SERVICE_ACCOUNT_EMAIL"
  QA_JIRA_API_TOKEN: "REPLACE_WITH_SECRET_MANAGER_VALUE"
  QA_BITBUCKET_ACCESS_TOKEN: "REPLACE_WITH_SECRET_MANAGER_VALUE"
