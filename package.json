{
  "name": "agents-via-docs",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:agent-window": "npm run dev:all --workspace @agents-via-docs/agent-window",
    "dev:agent-window:api": "npm run dev --workspace @agents-via-docs/agent-window",
    "dev:agent-window:ui": "npm run dev:client --workspace @agents-via-docs/agent-window",
    "build:agent-window": "npm run build --workspace @agents-via-docs/agent-window",
    "db:push": "npm run db:push --workspace @agents-via-docs/agent-window"
  },
  "engines": {
    "node": ">=20.18.0"
  }
}
