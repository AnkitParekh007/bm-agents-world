version: 1.0.0
references:
- id: atlassian-reader
  provider: vault-or-cloud-secret-manager
  reference: REPLACE_WITH_REFERENCE
  exposure: adapter-only
- id: bitbucket-docs-writer
  provider: vault-or-workload-identity
  reference: REPLACE_WITH_REFERENCE
  exposure: adapter-only
- id: docs-platform-publisher
  provider: vault-or-workload-identity
  reference: REPLACE_WITH_REFERENCE
  exposure: adapter-only
- id: localization-handoff
  provider: vault-or-workload-identity
  reference: REPLACE_WITH_REFERENCE
  exposure: adapter-only
- id: analytics-reader
  provider: vault-or-workload-identity
  reference: REPLACE_WITH_REFERENCE
  exposure: adapter-only
