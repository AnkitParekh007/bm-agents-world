version: 1.0.0
references:
- id: atlassian-capability
  provider: <VAULT_PROVIDER>
  path: <VAULT_PATH>/product/atlassian
  exposure: adapter-only
  rotation: 90d-or-shorter
- id: product-discovery-capability
  provider: <VAULT_PROVIDER>
  path: <VAULT_PATH>/product/discovery
  exposure: adapter-only
  rotation: 90d-or-shorter
- id: analytics-reader
  provider: <VAULT_PROVIDER>
  path: <VAULT_PATH>/product/analytics-read
  exposure: adapter-only
  rotation: dynamic-preferred
- id: feedback-reader
  provider: <VAULT_PROVIDER>
  path: <VAULT_PATH>/product/feedback-read
  exposure: adapter-only
  rotation: dynamic-preferred
- id: experimentation-capability
  provider: <VAULT_PROVIDER>
  path: <VAULT_PATH>/product/experimentation
  exposure: adapter-only
  rotation: short-lived
- id: collaboration-publisher
  provider: <VAULT_PROVIDER>
  path: <VAULT_PATH>/product/collaboration
  exposure: adapter-only
  rotation: 90d-or-shorter
- id: artifact-store
  provider: <VAULT_PROVIDER>
  path: <VAULT_PATH>/product/artifact-store
  exposure: adapter-only
  rotation: dynamic-preferred
